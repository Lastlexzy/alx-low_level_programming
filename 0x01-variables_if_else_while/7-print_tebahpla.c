#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 is valid (success)
 */

int main(void)
{
	char a;

	a = 'z';
	while
		(a >= 'a') {
			putchar(a);
			a--;
		}
	putchar('\n');
	return (0);
}
