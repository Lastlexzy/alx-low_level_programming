#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 is valid (success)
 */

int main(void)
{
	char d;

	d = 'z';
	while
		(d >= 'd') {
			putchar(d);
			d--;
		}
	putchar('\n');
	return (d);
}
